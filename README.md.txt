README.md
# Smart Monitoring with Predictive Auto-Scaling

This project demonstrates a simple predictive auto-scaler in Python:

- **Monitoring** CPU usage (`psutil`)
- **Predicting** future CPU load (linear regression)
- **Scaling** instance count based on predicted load

## Requirements
```bash
pip install -r requirements.txt
